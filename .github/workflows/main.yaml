name: First workflow
on: [push]

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo a message
        run: echo "Hello world"
      - name: Multiple commands
        run: |
          node -v
          npm -v